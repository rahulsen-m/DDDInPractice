- In this commit we added readonly static field for each coin and note 
- using c# new feature we added the static class reference in the file and using the readonly property directly.
using static DDDInPractice.Logic.Money;
so instead of writing Money.None this will only become None as we added the static using statement